env: "local" # local, develop, prod

storage_path: "./storage/storage.db"

http_server:
    address: "localhost:8081"
    timeout: 4s                   # time to read and send a request
    idle_timeout: 60s            # client connection lifetime
